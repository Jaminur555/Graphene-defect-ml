[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "graphene-defect-ml"
version = "0.1.0"
requires-python = ">=3.9"
dependencies = [
    "numpy",
    "ase",
]

[tool.setuptools.packages.find]
where = ["src"]
